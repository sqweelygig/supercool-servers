// TODO Sort out github sites deployment
// TODO Style for desktop resolutions

import { createApp } from "vue";
import SuperCoolServers from "./SuperCoolServers.vue";

createApp(SuperCoolServers).mount("#app");
